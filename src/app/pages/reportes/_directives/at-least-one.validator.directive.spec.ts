import { AtLeastOne.ValidatorDirective } from './at-least-one.validator.directive';

describe('AtLeastOne.ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AtLeastOne.ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
